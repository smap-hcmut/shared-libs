# Auth Package

JWT utilities with trace context integration.

## Features

- JWT validation with trace context logging
- Auth middleware with trace_id integration
- Backward compatible with existing auth usage
- Built on golang-jwt/jwt

## Components

- `jwt.go` - JWT validation utilities with trace context
- `middleware.go` - Authentication middleware